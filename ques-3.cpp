#include <stdio.h>
int main() {
    int i;
    int arr[5] = {1};   // initialize first element as 1, rest default to 0
    for (i = 0; i < 5; i++)
        printf("%d", arr[i]);
    return 0;
}


arr = {1, 0, 0, 0, 0}

output:
10000
